node_modules/
*.log
.DS_Store
Thumbs.db
desktop.ini

# QA artifacts — regenerate from screenshot.mjs
decks/**/screenshots/

# Slidev build output
decks/**/dist/

# Editor
.vscode/
.idea/
*.swp

# Scratch / one-off scripts written during work
.scratch-*
.tmp/
.tmp-*
bankrag-*.js
.claude/wf-*.js

# Local databases / ruflo artifacts — never commit
*.db

# Stray downloads / data exports accidentally dropped into deck folders
*.exe
decks/**/*.csv

# Downloaded binaries / tools (ffmpeg, yt-dlp, chrome profile, cert bundles)
.tools/

# Secrets — never commit
.env
.env.*
*.key
*.pem
